{
  "name": "man-search-provider",
  "version": "1.0.0",
  "description": "Search man pages from GNOME Shell",
  "type": "module",
  "private": true,
  "config": {
    "ID": "man-search-provider@example.github.com",
    "DIST_DIR": "./dist",
    "EXTENSION_DIR": "~/.local/share/gnome-shell/extensions",
    "TERMINAL": "gnome-terminal",
    "TERMINAL_PARAM": "--"
  },
  "scripts": {
    "build": "npm run project:build-dist",
    "install": "npm run project:install-extension",
    "clear": "npm run project:clear-dist; npm run project:clear-extension",
    "debug": "npm run nested-shell:restart",
    "dev": "npm run build && npm run install && npm run debug",

    "project:clear-dist": "[ -e $npm_package_config_DIST_DIR ] && rm -rf $npm_package_config_DIST_DIR",
    "project:clear-extension": "[ -e `eval echo \"$npm_package_config_EXTENSION_DIR/$npm_package_config_ID\"` ] && rm -rf `eval echo \"$npm_package_config_EXTENSION_DIR/$npm_package_config_ID\"`",
    "project:build-dist": "npm run project:clear-dist; echo 'Compile...'; tsc && echo '... done'",
    "project:mkdir-extension": "mkdir -p `eval echo \"$npm_package_config_EXTENSION_DIR/$npm_package_config_ID\"`",
    "project:install-extension": "[ -e $npm_package_config_DIST_DIR ] && { npm run project:clear-extension; npm run project:mkdir-extension && echo 'Install...'; cp -r ./dist/* metadata.json `eval echo \"$npm_package_config_EXTENSION_DIR/$npm_package_config_ID/\"` && echo '... done'; } || echo 'ERROR: Nothing to install. Run \"npm run build\" first'",

    "nested-shell:start": "$npm_package_config_TERMINAL --title='gnome-shell debug output' $npm_package_config_TERMINAL_PARAM bash -c 'env GNOME_SHELL_SLOWDOWN_FACTOR=2 MUTTER_DEBUG_DUMMY_MODE_SPECS=1280x720 G_MESSAGES_DEBUG=GNOME\\ Shell dbus-run-session -- gnome-shell --nested --wayland' &>/dev/null; echo '...Nested Shell started'",
    "nested-shell:stop": "pkill -f 'gnome-shell --nested --wayland' && echo '...Nested Shell stopped'",
    "nested-shell:restart": "npm run nested-shell:stop; sleep 1; npm run nested-shell:start"

  },
  "devDependencies": {
    "eslint": "^9.37.0",
    "eslint-plugin-jsdoc": "^61.1.0",
    "typescript": "^5.9.3",
    "@girs/gnome-shell": "^49.0.2",
    "@girs/gjs": "^4.0.0-beta.38",
    "@girs/st-17": "^17.0.0-4.0.0-beta.38"
  }
}
